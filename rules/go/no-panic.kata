rule no-panic {
  lang go

  match call_expression @match {
    function: identifier @fn
  }

  where {
    text(@fn) == "panic"
  }

  emit @match {
    message "panic is not allowed - return a domain-specific error and let the caller decide"
  }
}
