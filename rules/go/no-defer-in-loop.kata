rule no-defer-in-loop {
  lang go

  match defer_statement @match

  where {
    inside @match for_statement until func_literal
  }

  emit @match {
    message "defer in a loop runs only when the function returns, not each iteration - extract the body into its own function or release the resource explicitly"
  }
}
