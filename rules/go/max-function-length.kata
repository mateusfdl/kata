rule max-function-length {
  lang go

  match [function_declaration, method_declaration, func_literal] @match

  where {
    length(@match) > 50
  }

  emit @match {
    message "function spans {length(@match)} lines (max 50) - split it into focused units"
  }
}
