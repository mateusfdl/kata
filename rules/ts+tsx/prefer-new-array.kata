rule prefer-new-array {
  lang ts, tsx

  match call_expression @match {
    function: identifier @fn
  }

  where {
    text(@fn) == "Array"
  }

  emit @match {
    message "Use new Array() instead of Array()"
  }
}
