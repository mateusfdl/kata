rule max-nesting {
  lang ts

  match [function_declaration, function_expression, generator_function_declaration, generator_function, arrow_function, method_definition] @match

  where {
    nesting(@match) > 3
  }

  emit @match {
    message "nesting depth {nesting(@match)} exceeds 3 - flatten with guard clauses"
  }
}
