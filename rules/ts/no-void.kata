rule no-void {
  lang ts

  match unary_expression @match {
    operator: "void"
  }

  where {
    text(@match) not in ["void 0", "void(0)"]
    not parent @match expression_statement
  }

  emit @match {
    message "void operator is not allowed - use undefined or remove it"
  }
}
